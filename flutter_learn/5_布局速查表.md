# Flutter 布局速查表

## 🎯 一句话总结

- **Column** = 竖着排（从上到下）
- **Row** = 横着排（从左到右）
- **children** = 子控件列表

---

## 📊 Column vs Row 对比

```dart
// 垂直排列
Column(
  children: [
    Text('A'),
    Text('B'),
    Text('C'),
  ],
)

// 水平排列
Row(
  children: [
    Text('A'),
    Text('B'),
    Text('C'),
  ],
)
```

### 效果对比：
```
Column              Row
┌─────┐            ┌─────────┐
│  A  │            │ A  B  C │
│  B  │            └─────────┘
│  C  │
└─────┘
```

---

## 🎨 主轴对齐（mainAxisAlignment）

### Column 的主轴 = 垂直方向

```dart
// 1. start - 顶部
Column(
  mainAxisAlignment: MainAxisAlignment.start,
  children: [Text('A'), Text('B')],
)

// 2. center - 居中
Column(
  mainAxisAlignment: MainAxisAlignment.center,
  children: [Text('A'), Text('B')],
)

// 3. end - 底部
Column(
  mainAxisAlignment: MainAxisAlignment.end,
  children: [Text('A'), Text('B')],
)

// 4. spaceBetween - 两端对齐
Column(
  mainAxisAlignment: MainAxisAlignment.spaceBetween,
  children: [Text('A'), Text('B'), Text('C')],
)

// 5. spaceAround - 均匀分布
Column(
  mainAxisAlignment: MainAxisAlignment.spaceAround,
  children: [Text('A'), Text('B'), Text('C')],
)

// 6. spaceEvenly - 完全均匀
Column(
  mainAxisAlignment: MainAxisAlignment.spaceEvenly,
  children: [Text('A'), Text('B'), Text('C')],
)
```

### 效果图：
```
start    center    end      spaceBetween  spaceAround  spaceEvenly
┌───┐    ┌───┐    ┌───┐    ┌───┐         ┌───┐        ┌───┐
│ A │    │   │    │   │    │ A │         │   │        │   │
│ B │    │ A │    │   │    │   │         │ A │        │ A │
│   │    │ B │    │ A │    │ B │         │   │        │   │
│   │    │   │    │ B │    │   │         │ B │        │ B │
│   │    │   │    │   │    │ C │         │   │        │   │
└───┘    └───┘    └───┘    └───┘         │ C │        │ C │
                                          │   │        │   │
                                          └───┘        └───┘
```

---

## 🎨 交叉轴对齐（crossAxisAlignment）

### Column 的交叉轴 = 水平方向

```dart
// 1. start - 左对齐
Column(
  crossAxisAlignment: CrossAxisAlignment.start,
  children: [Text('短'), Text('比较长的文字')],
)

// 2. center - 居中
Column(
  crossAxisAlignment: CrossAxisAlignment.center,
  children: [Text('短'), Text('比较长的文字')],
)

// 3. end - 右对齐
Column(
  crossAxisAlignment: CrossAxisAlignment.end,
  children: [Text('短'), Text('比较长的文字')],
)
```

### 效果图：
```
start              center             end
┌──────────┐      ┌──────────┐      ┌──────────┐
│短        │      │    短    │      │        短│
│比较长的文字│      │比较长的文字│      │比较长的文字│
└──────────┘      └──────────┘      └──────────┘
```

---

## 🎨 Row 的对齐方式

### Row 的主轴 = 水平方向

```dart
// 左对齐
Row(
  mainAxisAlignment: MainAxisAlignment.start,
  children: [Text('A'), Text('B')],
)

// 居中
Row(
  mainAxisAlignment: MainAxisAlignment.center,
  children: [Text('A'), Text('B')],
)

// 右对齐
Row(
  mainAxisAlignment: MainAxisAlignment.end,
  children: [Text('A'), Text('B')],
)

// 两端对齐
Row(
  mainAxisAlignment: MainAxisAlignment.spaceBetween,
  children: [Text('A'), Text('B'), Text('C')],
)
```

### 效果图：
```
start           center          end             spaceBetween
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│A B      │    │   A B   │    │      A B│    │A    B   C│
└─────────┘    └─────────┘    └─────────┘    └─────────┘
```

---

## 📏 添加间距的方法

### 方法1：SizedBox（推荐）

```dart
// 垂直间距
Column(
  children: [
    Text('第一行'),
    SizedBox(height: 20),  // 间距 20
    Text('第二行'),
  ],
)

// 水平间距
Row(
  children: [
    Text('左边'),
    SizedBox(width: 20),  // 间距 20
    Text('右边'),
  ],
)
```

### 方法2：Padding

```dart
// 四周间距
Padding(
  padding: EdgeInsets.all(20),
  child: Text('有间距的文字'),
)

// 单独设置
Padding(
  padding: EdgeInsets.only(
    left: 10,
    right: 10,
    top: 20,
    bottom: 20,
  ),
  child: Text('文字'),
)

// 对称间距
Padding(
  padding: EdgeInsets.symmetric(
    horizontal: 20,  // 左右
    vertical: 10,    // 上下
  ),
  child: Text('文字'),
)
```

---

## 🔄 嵌套布局

### Column 里放 Row

```dart
Column(
  children: [
    Text('标题'),
    Row(
      children: [
        Icon(Icons.star),
        Text('5.0'),
      ],
    ),
    Text('描述'),
  ],
)
```

### 效果：
```
┌──────────┐
│  标题    │
│ ⭐ 5.0   │
│  描述    │
└──────────┘
```

### Row 里放 Column

```dart
Row(
  children: [
    Column(
      children: [
        Icon(Icons.home),
        Text('首页'),
      ],
    ),
    Column(
      children: [
        Icon(Icons.person),
        Text('我的'),
      ],
    ),
  ],
)
```

### 效果：
```
┌──────────────┐
│ 🏠    👤     │
│ 首页   我的   │
└──────────────┘
```

---

## 🎯 常用组合

### 1. 居中显示
```dart
Column(
  mainAxisAlignment: MainAxisAlignment.center,
  crossAxisAlignment: CrossAxisAlignment.center,
  children: [Text('居中显示')],
)
```

### 2. 顶部左对齐
```dart
Column(
  mainAxisAlignment: MainAxisAlignment.start,
  crossAxisAlignment: CrossAxisAlignment.start,
  children: [Text('顶部左对齐')],
)
```

### 3. 底部右对齐
```dart
Column(
  mainAxisAlignment: MainAxisAlignment.end,
  crossAxisAlignment: CrossAxisAlignment.end,
  children: [Text('底部右对齐')],
)
```

### 4. 两端对齐
```dart
Row(
  mainAxisAlignment: MainAxisAlignment.spaceBetween,
  children: [
    Text('左边'),
    Text('右边'),
  ],
)
```

---

## 📱 实战模板

### 模板1：标题 + 内容 + 按钮
```dart
Column(
  children: [
    Text('标题', style: TextStyle(fontSize: 24)),
    SizedBox(height: 20),
    Text('这是内容描述'),
    SizedBox(height: 30),
    ElevatedButton(
      onPressed: () {},
      child: Text('按钮'),
    ),
  ],
)
```

### 模板2：图标 + 文字（横向）
```dart
Row(
  children: [
    Icon(Icons.star),
    SizedBox(width: 5),
    Text('5.0'),
  ],
)
```

### 模板3：左右布局
```dart
Row(
  mainAxisAlignment: MainAxisAlignment.spaceBetween,
  children: [
    Text('价格：¥99'),
    Text('库存：100'),
  ],
)
```

### 模板4：卡片布局
```dart
Container(
  padding: EdgeInsets.all(20),
  child: Column(
    crossAxisAlignment: CrossAxisAlignment.start,
    children: [
      Text('标题', style: TextStyle(fontSize: 20)),
      SizedBox(height: 10),
      Text('描述信息'),
      SizedBox(height: 20),
      Row(
        mainAxisAlignment: MainAxisAlignment.end,
        children: [
          TextButton(onPressed: () {}, child: Text('取消')),
          ElevatedButton(onPressed: () {}, child: Text('确定')),
        ],
      ),
    ],
  ),
)
```

---

## 🎨 对比 Android

| Android | Flutter | 说明 |
|---------|---------|------|
| LinearLayout (vertical) | Column | 垂直布局 |
| LinearLayout (horizontal) | Row | 水平布局 |
| android:gravity="center" | mainAxisAlignment: center | 主轴居中 |
| android:layout_gravity | crossAxisAlignment | 交叉轴对齐 |
| android:layout_margin | Padding / SizedBox | 间距 |
| android:layout_weight | Expanded | 权重 |

---

## 💡 记忆技巧

1. **Column = 竖着排**（想象成一列）
2. **Row = 横着排**（想象成一行）
3. **main = 主方向**（Column 是竖，Row 是横）
4. **cross = 交叉方向**（Column 是横，Row 是竖）
5. **children = []**（方括号，里面放子控件）

---

## 🚀 快速开始

### 第一步：选择布局方向
- 竖着排 → 用 `Column`
- 横着排 → 用 `Row`

### 第二步：添加子控件
```dart
Column(
  children: [
    // 在这里添加子控件
  ],
)
```

### 第三步：调整对齐
```dart
Column(
  mainAxisAlignment: MainAxisAlignment.center,  // 主轴
  crossAxisAlignment: CrossAxisAlignment.start,  // 交叉轴
  children: [],
)
```

### 第四步：添加间距
```dart
Column(
  children: [
    Text('第一个'),
    SizedBox(height: 20),  // 间距
    Text('第二个'),
  ],
)
```

---

## 📚 下一步

1. 打开 `layout_examples.dart` 看完整示例
2. 尝试修改对齐方式，观察效果
3. 练习嵌套布局
4. 做一个自己的页面布局
