# Flutter 页面编写教程（小白版）

## 🎯 核心：Flutter 页面 = Dart 代码（不是 XML）

### Android vs Flutter
- **Android**: XML 写布局 + Java/Kotlin 写逻辑
- **Flutter**: 全部用 Dart 代码写（布局 + 逻辑）

---

## 📱 第一步：最简单的页面

```dart
import 'package:flutter/material.dart';

// 1. 创建一个页面类（继承 StatelessWidget）
class MyFirstPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // 2. 返回页面内容
    return Scaffold(
      appBar: AppBar(
        title: Text('我的第一个页面'),
      ),
      body: Center(
        child: Text('Hello Flutter!'),
      ),
    );
  }
}
```

### 代码解释：
- `StatelessWidget` = 静态页面（内容不变）
- `build()` = 构建页面的方法（类似 Android 的 setContentView）
- `Scaffold` = 页面框架（提供 AppBar、Body 等）
- `AppBar` = 顶部标题栏
- `body` = 页面主体内容

---

## 📱 第二步：添加按钮和交互

```dart
import 'package:flutter/material.dart';

class MySecondPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('按钮示例'),
        backgroundColor: Colors.blue,
      ),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            print('按钮被点击了！');
          },
          child: Text('点我'),
        ),
      ),
    );
  }
}
```

### 对比 Android：
```xml
<!-- Android XML -->
<Button
    android:id="@+id/myButton"
    android:text="点我" />
```

```dart
// Flutter Dart
ElevatedButton(
  onPressed: () { print('点击'); },
  child: Text('点我'),
)
```

---

## 📱 第三步：垂直布局（Column）

```dart
class MyThirdPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('垂直布局')),
      body: Column(
        children: [
          Text('第一行文字'),
          Text('第二行文字'),
          ElevatedButton(
            onPressed: () {},
            child: Text('按钮'),
          ),
        ],
      ),
    );
  }
}
```

### 对比 Android：
- `Column` = `LinearLayout` (vertical)
- `Row` = `LinearLayout` (horizontal)
- `Stack` = `FrameLayout`

---

## 📱 第四步：有状态的页面（可以改变内容）

```dart
// 1. 继承 StatefulWidget（可变页面）
class CounterPage extends StatefulWidget {
  @override
  _CounterPageState createState() => _CounterPageState();
}

// 2. 创建状态类
class _CounterPageState extends State<CounterPage> {
  int count = 0; // 计数器

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('计数器')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text(
              '点击次数：',
              style: TextStyle(fontSize: 20),
            ),
            Text(
              '$count',
              style: TextStyle(fontSize: 50, color: Colors.blue),
            ),
            SizedBox(height: 20),
            ElevatedButton(
              onPressed: () {
                setState(() {
                  count++; // 点击后数字加1
                });
              },
              child: Text('点我 +1'),
            ),
          ],
        ),
      ),
    );
  }
}
```

### 关键点：
- `StatefulWidget` = 可变页面
- `setState(() {})` = 刷新页面（类似 Android 的 notifyDataSetChanged）

---

## 📱 第五步：输入框 + 按钮

```dart
class LoginPage extends StatefulWidget {
  @override
  _LoginPageState createState() => _LoginPageState();
}

class _LoginPageState extends State<LoginPage> {
  // 控制器（获取输入框内容）
  TextEditingController usernameController = TextEditingController();
  TextEditingController passwordController = TextEditingController();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('登录页面')),
      body: Padding(
        padding: EdgeInsets.all(20),
        child: Column(
          children: [
            // 用户名输入框
            TextField(
              controller: usernameController,
              decoration: InputDecoration(
                labelText: '用户名',
                border: OutlineInputBorder(),
              ),
            ),
            SizedBox(height: 20),
            // 密码输入框
            TextField(
              controller: passwordController,
              obscureText: true, // 密码隐藏
              decoration: InputDecoration(
                labelText: '密码',
                border: OutlineInputBorder(),
              ),
            ),
            SizedBox(height: 30),
            // 登录按钮
            ElevatedButton(
              onPressed: () {
                String username = usernameController.text;
                String password = passwordController.text;
                print('用户名：$username, 密码：$password');
              },
              child: Text('登录'),
              style: ElevatedButton.styleFrom(
                minimumSize: Size(double.infinity, 50),
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```

---

## 🎨 常用组件对照表

| Android | Flutter |
|---------|---------|
| TextView | Text() |
| EditText | TextField() |
| Button | ElevatedButton() / TextButton() |
| ImageView | Image.asset() / Image.network() |
| LinearLayout (vertical) | Column() |
| LinearLayout (horizontal) | Row() |
| FrameLayout | Stack() |
| ScrollView | SingleChildScrollView() |
| ListView | ListView() |
| Padding | Padding() |
| Margin | Container(margin: ...) |

---

## 🚀 如何运行你的页面？

### 在 main.dart 中设置：

```dart
import 'package:flutter/material.dart';
import 'my_first_page.dart'; // 导入你的页面

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      home: MyFirstPage(), // ← 设置首页
    );
  }
}
```

---

## 💡 小白必知的 3 个概念

### 1. Widget = 组件
Flutter 里一切都是 Widget（文字、按钮、布局都是）

### 2. StatelessWidget vs StatefulWidget
- **Stateless** = 静态页面（内容不变）
- **Stateful** = 动态页面（内容会变）

### 3. build() 方法
每个页面都有 `build()` 方法，返回页面内容

---

## 📚 下一步学习

1. 看你项目里的 `trip_flutter/lib/pages/login_page.dart`
2. 尝试修改文字、颜色
3. 添加一个按钮
4. 学习布局：Column、Row、Container

记住：**Flutter 不用 XML，全部用 Dart 代码写！**
