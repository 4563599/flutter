# Flutter 子控件布局教程（小白版）

## 🎯 核心概念：children = 子控件列表

在 Flutter 中，子控件都放在 `children: []` 里面。

---

## 📱 一、垂直排列（Column）

### 基础用法
```dart
Column(
  children: [
    Text('第一行'),
    Text('第二行'),
    Text('第三行'),
  ],
)
```

### 效果：
```
┌─────────┐
│ 第一行   │
│ 第二行   │
│ 第三行   │
└─────────┘
```

### 完整示例
```dart
class VerticalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('垂直布局')),
      body: Column(
        children: [
          Text('我是第一个'),
          Text('我是第二个'),
          ElevatedButton(
            onPressed: () {},
            child: Text('按钮'),
          ),
          Icon(Icons.star, size: 50),
        ],
      ),
    );
  }
}
```

---

## 📱 二、水平排列（Row）

### 基础用法
```dart
Row(
  children: [
    Text('左边'),
    Text('中间'),
    Text('右边'),
  ],
)
```

### 效果：
```
┌──────────────────┐
│ 左边  中间  右边  │
└──────────────────┘
```

### 完整示例
```dart
class HorizontalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('水平布局')),
      body: Row(
        children: [
          Icon(Icons.home),
          Text('首页'),
          Icon(Icons.person),
          Text('我的'),
        ],
      ),
    );
  }
}
```

---

## 📱 三、控制子控件位置

### 1. 主轴对齐（mainAxisAlignment）

#### Column 的主轴 = 垂直方向
```dart
Column(
  mainAxisAlignment: MainAxisAlignment.start,  // 顶部对齐（默认）
  children: [Text('A'), Text('B')],
)

Column(
  mainAxisAlignment: MainAxisAlignment.center,  // 居中
  children: [Text('A'), Text('B')],
)

Column(
  mainAxisAlignment: MainAxisAlignment.end,  // 底部对齐
  children: [Text('A'), Text('B')],
)

Column(
  mainAxisAlignment: MainAxisAlignment.spaceBetween,  // 两端对齐
  children: [Text('A'), Text('B'), Text('C')],
)

Column(
  mainAxisAlignment: MainAxisAlignment.spaceAround,  // 均匀分布
  children: [Text('A'), Text('B'), Text('C')],
)
```

#### 效果对比：
```
start           center          end
┌─────┐        ┌─────┐        ┌─────┐
│ A   │        │     │        │     │
│ B   │        │  A  │        │     │
│     │        │  B  │        │  A  │
│     │        │     │        │  B  │
└─────┘        └─────┘        └─────┘

spaceBetween    spaceAround
┌─────┐        ┌─────┐
│ A   │        │  A  │
│     │        │     │
│ B   │        │  B  │
│     │        │     │
│ C   │        │  C  │
└─────┘        └─────┘
```

### 2. 交叉轴对齐（crossAxisAlignment）

#### Column 的交叉轴 = 水平方向
```dart
Column(
  crossAxisAlignment: CrossAxisAlignment.start,  // 左对齐（默认）
  children: [Text('短'), Text('比较长的文字')],
)

Column(
  crossAxisAlignment: CrossAxisAlignment.center,  // 居中
  children: [Text('短'), Text('比较长的文字')],
)

Column(
  crossAxisAlignment: CrossAxisAlignment.end,  // 右对齐
  children: [Text('短'), Text('比较长的文字')],
)
```

#### 效果对比：
```
start              center             end
┌──────────┐      ┌──────────┐      ┌──────────┐
│短        │      │    短    │      │        短│
│比较长的文字│      │比较长的文字│      │比较长的文字│
└──────────┘      └──────────┘      └──────────┘
```

---

## 📱 四、Row 的对齐方式

### Row 的主轴 = 水平方向
```dart
Row(
  mainAxisAlignment: MainAxisAlignment.start,  // 左对齐（默认）
  children: [Text('A'), Text('B')],
)

Row(
  mainAxisAlignment: MainAxisAlignment.center,  // 居中
  children: [Text('A'), Text('B')],
)

Row(
  mainAxisAlignment: MainAxisAlignment.end,  // 右对齐
  children: [Text('A'), Text('B')],
)

Row(
  mainAxisAlignment: MainAxisAlignment.spaceBetween,  // 两端对齐
  children: [Text('A'), Text('B'), Text('C')],
)
```

### Row 的交叉轴 = 垂直方向
```dart
Row(
  crossAxisAlignment: CrossAxisAlignment.start,  // 顶部对齐
  children: [Text('A'), Text('B')],
)

Row(
  crossAxisAlignment: CrossAxisAlignment.center,  // 垂直居中
  children: [Text('A'), Text('B')],
)

Row(
  crossAxisAlignment: CrossAxisAlignment.end,  // 底部对齐
  children: [Text('A'), Text('B')],
)
```

---

## 📱 五、嵌套布局（Column 里面放 Row）

```dart
Column(
  children: [
    Text('标题'),
    Row(
      children: [
        Icon(Icons.star),
        Text('5.0'),
      ],
    ),
    Text('描述信息'),
  ],
)
```

### 效果：
```
┌──────────┐
│  标题    │
│ ⭐ 5.0   │
│ 描述信息  │
└──────────┘
```

---

## 📱 六、添加间距

### 方法1：SizedBox（推荐）
```dart
Column(
  children: [
    Text('第一行'),
    SizedBox(height: 20),  // 垂直间距 20
    Text('第二行'),
  ],
)

Row(
  children: [
    Text('左边'),
    SizedBox(width: 20),  // 水平间距 20
    Text('右边'),
  ],
)
```

### 方法2：Padding
```dart
Column(
  children: [
    Padding(
      padding: EdgeInsets.all(10),  // 四周间距 10
      child: Text('有间距的文字'),
    ),
  ],
)
```

---

## 📱 七、完整实战示例

### 示例1：个人信息卡片
```dart
class ProfileCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('个人信息')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // 头像
            Icon(Icons.account_circle, size: 100, color: Colors.blue),
            SizedBox(height: 20),
            // 名字
            Text(
              '张三',
              style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
            ),
            SizedBox(height: 10),
            // 信息行
            Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                Icon(Icons.email, size: 16),
                SizedBox(width: 5),
                Text('zhangsan@example.com'),
              ],
            ),
            SizedBox(height: 10),
            Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                Icon(Icons.phone, size: 16),
                SizedBox(width: 5),
                Text('138-0000-0000'),
              ],
            ),
          ],
        ),
      ),
    );
  }
}
```

### 示例2：底部导航栏
```dart
class BottomNavBar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('底部导航')),
      body: Center(child: Text('页面内容')),
      bottomNavigationBar: Container(
        height: 60,
        color: Colors.white,
        child: Row(
          mainAxisAlignment: MainAxisAlignment.spaceAround,
          children: [
            // 首页
            Column(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                Icon(Icons.home),
                Text('首页', style: TextStyle(fontSize: 12)),
              ],
            ),
            // 分类
            Column(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                Icon(Icons.category),
                Text('分类', style: TextStyle(fontSize: 12)),
              ],
            ),
            // 购物车
            Column(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                Icon(Icons.shopping_cart),
                Text('购物车', style: TextStyle(fontSize: 12)),
              ],
            ),
            // 我的
            Column(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                Icon(Icons.person),
                Text('我的', style: TextStyle(fontSize: 12)),
              ],
            ),
          ],
        ),
      ),
    );
  }
}
```

---

## 🎨 对比 Android

| Android | Flutter |
|---------|---------|
| LinearLayout (vertical) | Column |
| LinearLayout (horizontal) | Row |
| android:gravity="center" | mainAxisAlignment: MainAxisAlignment.center |
| android:layout_gravity | crossAxisAlignment |
| android:layout_margin | Padding / SizedBox |

---

## 💡 记忆口诀

1. **Column = 竖着排**（从上到下）
2. **Row = 横着排**（从左到右）
3. **mainAxis = 主方向**（Column 是竖，Row 是横）
4. **crossAxis = 交叉方向**（Column 是横，Row 是竖）
5. **children = 子控件列表**（用方括号 []）

---

## 🚀 快速参考

### Column 常用属性
```dart
Column(
  mainAxisAlignment: MainAxisAlignment.center,  // 主轴对齐
  crossAxisAlignment: CrossAxisAlignment.start,  // 交叉轴对齐
  mainAxisSize: MainAxisSize.min,  // 主轴尺寸（min/max）
  children: [],  // 子控件列表
)
```

### Row 常用属性
```dart
Row(
  mainAxisAlignment: MainAxisAlignment.spaceBetween,  // 主轴对齐
  crossAxisAlignment: CrossAxisAlignment.center,  // 交叉轴对齐
  mainAxisSize: MainAxisSize.max,  // 主轴尺寸
  children: [],  // 子控件列表
)
```

### 对齐方式速查
- `start` - 开始位置
- `center` - 居中
- `end` - 结束位置
- `spaceBetween` - 两端对齐，中间均分
- `spaceAround` - 均匀分布
- `spaceEvenly` - 完全均匀分布

---

## 📚 下一步

1. 打开 `flutter_learn/layout_examples.dart` 看实战代码
2. 尝试修改对齐方式，看效果变化
3. 练习嵌套布局（Column 里放 Row）
